import 'package:flutter/cupertino.dart';

class RecordsController extends StatelessWidget {
  const RecordsController({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("data1"),
      ),
    );
  }
}
